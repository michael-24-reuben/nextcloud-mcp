# Notes

## 2026-06-26

- Activated after `2026-06-26-mvp-build-foundation` was resolved.
- The required modules do not exist yet:
  - `lib/nextcloud-mcp-core`
  - `lib/nextcloud-mcp-http`
  - `lib/nextcloud-mcp-config`
- Per the recorded project boundary, implementation should not create those module directories unless the user creates them or explicitly authorizes module creation.
- Audio callout was played from `scratch/callout/calling-to-michael.wav` when the blocker was reached.
