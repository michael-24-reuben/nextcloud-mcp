# Notes

## 2026-06-26

- Activated after `2026-06-26-mvp-build-foundation` was resolved.
- The required security module does not exist yet:
  - `lib/nextcloud-mcp-security`
- This architect also depends on the core primitives planned in `2026-06-26-mvp-core-http-config`.
- Per the recorded project boundary, implementation should not create the module directory unless the user creates it or explicitly authorizes module creation.
- Audio callout was played from `scratch/callout/calling-to-michael.wav` when the blocker was reached.
