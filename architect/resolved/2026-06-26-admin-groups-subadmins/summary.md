# Summary

Implemented admin group provisioning and subadmin management in `nextcloud-mcp-admin`. The admin facade now exposes `groups()` for group reads/mutations, membership changes, and subadmin promotion/demotion. Focused fake HTTP-backed tests passed.
