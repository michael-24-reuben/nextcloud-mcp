# Blockers

## RESOLVED: Security module directory was missing

The security policy baseline implementation needs:

- `lib/nextcloud-mcp-security`

It also depends on the core primitives from:

- `lib/nextcloud-mcp-core`

The user created the needed module directories and POMs. This blocker was cleared on 2026-06-26 before implementation.
