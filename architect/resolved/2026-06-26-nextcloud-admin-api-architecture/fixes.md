# Fixes

- Implemented the admin auth/client foundation in `nextcloud-mcp-admin`.
- Added user, group, subadmin, app, and share-boundary admin client surfaces.
- Added admin MCP tool registrations with dedicated scopes and risk metadata.
- Added a non-executing guarded OCC command-plan bridge.
- Updated root handoff state to return the next active queue to the MVP outbound architects.
