# Summary

Implemented admin-owned user provisioning in `nextcloud-mcp-admin`. The admin facade now exposes `users()` for list/search, arbitrary user lookup, creation, field updates, editable fields, enable/disable/delete, welcome resend, and read-only group/subadmin lookup. Focused fake HTTP-backed tests passed.
