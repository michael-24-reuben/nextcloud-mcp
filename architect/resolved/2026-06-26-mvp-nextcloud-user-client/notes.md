# Notes

- Live smoke testing already established that `temporary` is the user id/login and `tempo` is only the display name.
- The client must therefore keep account id, login username, current user id, and display name separate.
- WebDAV `MKCOL` collection targets should include a trailing slash.
