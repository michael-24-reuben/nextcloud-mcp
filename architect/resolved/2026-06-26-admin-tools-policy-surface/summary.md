# Summary

The admin MCP tool policy surface is implemented. Admin tools now expose dedicated admin scopes, risk metadata, and confirmation markers while routing through the `nextcloud-mcp-admin` client instead of the normal file/client API path.
