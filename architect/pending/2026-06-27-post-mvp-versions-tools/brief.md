# Brief

Implement the post-MVP Nextcloud file versions tool surface for listing available versions and restoring a selected version.

This should extend the existing non-admin WebDAV client path and preserve the same runtime and policy model as the MVP file tools.
