# Todo

- [ ] Review `lib/nextcloud-mcp-tool-catalog` module state.
- [ ] Decide catalog output format and stability guarantees.
- [ ] Normalize descriptor metadata across CLI, server, and MCP JSON-RPC listing.
- [ ] Add export writer behavior if needed.
- [ ] Add tests for descriptor normalization and output shape.
- [ ] Update docs only after implementation behavior is verified.
