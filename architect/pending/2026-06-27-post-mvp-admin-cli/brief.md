# Brief

Implement the post-MVP admin CLI surface on top of the resolved admin client and admin tool policy model.

The normal MVP CLI is resolved and intentionally excludes admin CLI behavior. This child should add admin operations without weakening the risk/confirmation boundaries already recorded for admin tools.
