# Brief

Create the post-MVP packaging, configuration, and operations documentation slice.

`config/` and `docs/` already exist, but the parent plan also calls for Docker packaging, config schemas/examples, scripts, installer or distribution support, and deployment documentation.
