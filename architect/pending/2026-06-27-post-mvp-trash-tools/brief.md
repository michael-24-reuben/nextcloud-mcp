# Brief

Implement the post-MVP Nextcloud trashbin tool surface for listing deleted items, restoring items, deleting trash items, and emptying trash.

This should build on the existing user client, files tools, runtime, and security policy instead of creating a parallel invocation path.
