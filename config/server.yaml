tools:
  tools:
    nextcloud.files:
      enabled: true
      requiredScopes:
        - nextcloud.files.read
      destructive: false
    nextcloud.shares:
      enabled: true
      requiredScopes:
        - nextcloud.shares.read
      destructive: false
    nextcloud.user:
      enabled: true
      requiredScopes:
        - nextcloud.user.read
      destructive: false
    nextcloud.admin:
      enabled: false
      requiredScopes:
        - nextcloud.admin.users.read
      destructive: true

admin:
  enabled: false
  accountId:
